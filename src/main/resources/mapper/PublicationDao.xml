<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miniprogram.zhihuicunwu.dao.PublicationDao">

    <resultMap type="com.miniprogram.zhihuicunwu.entity.Publication" id="PublicationMap">
        <result property="pid" column="pid" jdbcType="INTEGER"/>
        <result property="did" column="did" jdbcType="INTEGER"/>
        <result property="ptype" column="pType" jdbcType="INTEGER"/>
        <result property="pcontent" column="pContent" jdbcType="VARCHAR"/>
        <result property="ptime" column="pTime" jdbcType="TIMESTAMP"/>
        <result property="ptitle" column="pTitle" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PublicationMap">
        select
          pid, did, pType, pContent, pTime, pTitle
        from publication
        where pid = #{pid}
    </select>

    <!--根据标题模糊查询-->
    <select id="queryFuzzyByTitle" resultMap="PublicationMap">
        select
            pid, did, pType, pContent, pTime, pTitle
        from publication
        where pTitle like #{keywords}
        ORDER BY pTime desc, pid desc
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PublicationMap">
        select
          pid, did, pType, pContent, pTime, pTitle
        from publication
        order by ptime desc, pid desc
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--根据条件统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from publication
        <where>
            <if test="pid != null">
                and pid = #{pid}
            </if>
            <if test="did != null">
                and did = #{did}
            </if>
            <if test="ptype != null">
                and pType = #{ptype}
            </if>
            <if test="pcontent != null and pcontent != ''">
                and pContent = #{pcontent}
            </if>
            <if test="ptime != null">
                and pTime = #{ptime}
            </if>
            <if test="ptitle != null and ptitle != ''">
                pTitle = #{ptitle},
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="countAll" resultType="java.lang.Integer">
        select count(1)
        from publication
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="pid" useGeneratedKeys="true">
        insert into publication(did, pType, pContent, pTime, pTitle)
        values (#{did}, #{ptype}, #{pcontent}, #{ptime}, #{ptitle})
    </insert>

    <insert id="insertBatch" keyProperty="pid" useGeneratedKeys="true">
        insert into publication(did, pType, pContent, pTime, pTitle)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.did}, #{entity.ptype}, #{entity.pcontent}, #{entity.ptime}, #{entity.ptitle})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="pid" useGeneratedKeys="true">
        insert into publication(did, pType, pContent, pTime, pTitle)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.did}, #{entity.ptype}, #{entity.pcontent}, #{entity.ptime}, #{entity.ptitle})
        </foreach>
        on duplicate key update
        did = values(did),
        pType = values(pType),
        pContent = values(pContent),
        pTime = values(pTime),
        pTitle = values(pTitle)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update publication
        <set>
            <if test="did != null">
                did = #{did},
            </if>
            <if test="ptype != null">
                pType = #{ptype},
            </if>
            <if test="pcontent != null and pcontent != ''">
                pContent = #{pcontent},
            </if>
            <if test="ptime != null">
                pTime = #{ptime},
            </if>
            <if test="ptitle != null and ptitle != ''">
                pTitle = #{ptitle},
            </if>
        </set>
        where pid = #{pid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from publication where pid = #{pid}
    </delete>

</mapper>

